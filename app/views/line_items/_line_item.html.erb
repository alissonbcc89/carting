<!--<% @cart.line_items.each do |item| %> -->
<% if line_item == @current_item %>
<tr id="current_item">
<% else %>
  <tr>
  <% end %>
    <td><%= line_item.quantity %>&times;</td>
    <td><%= line_item.product.name %></td>
    <td class='item_price'><%= sprintf("R$%0.02f", line_item.total_price) %></td>
  </tr>
<!-- <% end %> -->
